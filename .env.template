# ============================================
# 🔐 API認証情報テンプレート
# ============================================
# このファイルをコピーして .env を作成し、実際のAPIキーを設定してください
#
# セットアップ手順:
# 1. このファイルを .env にコピー
# 2. 必要なAPIキーを取得して設定
# 3. .env は .gitignore で除外されています（安全）
#
# ============================================

# --------------------------------------------
# Gemini API（推奨 - 音声生成用）
# --------------------------------------------

# Gemini APIキー
# 取得方法:
#   1. https://makersuite.google.com/app/apikey にアクセス
#   2. Googleアカウントでログイン
#   3. [Create API Key] をクリック
#   4. APIキーをコピーして設定
#
# 用途: Gemini 2.5 Flash Preview TTS（音声生成）
# 利点: APIキーのみで利用可能、SSML不要、高品質な音声
GEMINI_API_KEY=

# --------------------------------------------
# Google Cloud Platform (GCP) - フォールバック/画像生成用
# --------------------------------------------

# GCP認証JSONファイルのパス
#
# 【推奨】階層型設定システムを使用:
#   グローバル: ~/.config/ai-agents/credentials/gcp/default.json
#   ローカル: ./ai-agents-config/credentials/gcp.json
#
# 設定方法:
#   1. launch_config_gui.command をダブルクリック
#   2. GUIで認証ファイルを設定
#
# 手動設定の場合:
#   1. https://console.cloud.google.com/
#   2. [IAMと管理] > [サービスアカウント]
#   3. キーを作成（JSON形式）
#   4. ~/.config/ai-agents/credentials/gcp/ に配置
GOOGLE_APPLICATION_CREDENTIALS=

# GCPプロジェクトID
# 例: my-project-12345
GCP_PROJECT_ID=

# Text-to-Speech API設定（GCP TTSフォールバック用）
# ※ Gemini TTSが優先されます。GCP認証がある場合のフォールバックとして使用
# 音声生成に使用する音声名（デフォルト: ja-JP-Neural2-B）
TTS_VOICE_NAME=ja-JP-Neural2-B
TTS_LANGUAGE_CODE=ja-JP

# Vertex AI Imagen API設定
# 画像生成に使用するモデル
IMAGEN_MODEL=imagegeneration@006
IMAGEN_LOCATION=us-central1

# --------------------------------------------
# GitHub
# --------------------------------------------

# GitHub Personal Access Token
# 取得方法:
# 1. https://github.com/settings/tokens
# 2. [Generate new token (classic)]
# 3. スコープ選択: repo, workflow, admin:public_key
# 4. トークンをコピーして設定
GITHUB_TOKEN=

# GitHubユーザー名
# 例: sohei-t
GITHUB_USERNAME=

# ポートフォリオリポジトリ名（デフォルト: ai-agent-portfolio）
GITHUB_PORTFOLIO_REPO=ai-agent-portfolio

# --------------------------------------------
# OpenAI（オプション）
# --------------------------------------------

# OpenAI APIキー
# 取得方法: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# 使用するモデル
OPENAI_MODEL=gpt-4

# --------------------------------------------
# その他のサービス（必要に応じて追加）
# --------------------------------------------

# Anthropic Claude API（オプション）
ANTHROPIC_API_KEY=

# Stripe（決済機能用、オプション）
STRIPE_API_KEY=
STRIPE_PUBLISHABLE_KEY=

# SendGrid（メール送信用、オプション）
SENDGRID_API_KEY=

# Twilio（SMS/通話用、オプション）
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# --------------------------------------------
# 環境設定
# --------------------------------------------

# 開発環境（development / staging / production）
NODE_ENV=development

# ポート番号（アプリケーション用）
PORT=3000

# ログレベル（debug / info / warn / error）
LOG_LEVEL=info

# --------------------------------------------
# 認証ファイルパス（階層型設定システム）
# --------------------------------------------
#
# 優先順位:
#   1. ローカル設定: ./ai-agents-config/local.env
#   2. プロファイル: ./ai-agents-config/profile.yaml で指定
#   3. グローバル: ~/.config/ai-agents/profiles/default.env
#
# 設定管理GUI:
#   streamlit run ~/.config/ai-agents/scripts/config_gui.py
#   または launch_config_gui.command をダブルクリック

# グローバル認証ファイルパス
GLOBAL_CREDENTIALS_PATH=~/.config/ai-agents/credentials

# ローカル認証ファイルパス（プロジェクト固有）
LOCAL_CREDENTIALS_PATH=./ai-agents-config/credentials
