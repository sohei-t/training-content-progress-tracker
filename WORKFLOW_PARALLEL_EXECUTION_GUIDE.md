# 📖 並列処理実行ガイド - Taskツール活用版

## 🎯 重要: なぜ並列処理が機能していないか

### 根本原因
**Claude APIのTaskツールを使用していない**
- 現状: メインClaudeがすべて直接実行
- 問題: サブエージェントが起動されない = 並列処理不可能

## ✅ 正しい並列処理の実装方法

### 1. Phase 3 (実装) の正しい実行方法

#### ❌ 間違った方法（現在の実行）
```python
# すべて順次実行されてしまう
# Frontend開発
create_frontend_files()
# Backend開発（Frontendの後）
create_backend_files()
# Database設計（Backendの後）
create_database_schema()
```

#### ✅ 正しい方法（Taskツール使用）
```python
# 単一メッセージで3つのTaskを同時起動
Task(
    description="Frontend Development",
    subagent_type="general-purpose",
    prompt="""
    【Frontend開発】
    作業ディレクトリ: ./worktrees/mission-v1

    タスク:
    1. index.html作成
    2. app.js作成
    3. style.css作成

    テスト合格まで実装を修正してください。
    """
)

Task(
    description="Backend Development",
    subagent_type="general-purpose",
    prompt="""
    【Backend開発】
    作業ディレクトリ: ./worktrees/mission-v1

    タスク:
    1. server.js作成
    2. API実装
    3. エラーハンドリング

    テスト合格まで実装を修正してください。
    """
)

Task(
    description="Database Design",
    subagent_type="general-purpose",
    prompt="""
    【Database設計】
    作業ディレクトリ: ./worktrees/mission-v1

    タスク:
    1. schema.sql作成
    2. migrations作成
    3. seed data作成

    スキーマが正しく機能することを確認してください。
    """
)
```

## 📊 実行パターン別ガイド

### Pattern A: 完全独立タスク（最大並列）
```
Phase 3: Frontend, Backend, Database
→ 3つ同時実行
→ 最も遅いタスクの時間で完了
```

### Pattern B: 部分依存タスク
```
Phase 2: Architecture設計とTest設計
→ Architecture完了後、TestとUIを並列実行
```

### Pattern C: クリティカルパス優先
```
Critical: 要件 → テスト → 実装
Non-critical: ドキュメント（並列可能）
```

## 🔧 実装例

### Phase別の正しい実行方法

#### Phase 1: 要件定義（単独）
```python
Task(
    description="Requirements Analysis",
    subagent_type="general-purpose",
    prompt="要件定義を作成..."
)
```

#### Phase 2: 設計（部分並列）
```python
# Architectureは単独
Task(
    description="Architecture Design",
    subagent_type="general-purpose",
    prompt="アーキテクチャ設計..."
)

# 完了後、TestとUIを並列
Task(
    description="Test Design",
    subagent_type="general-purpose",
    prompt="テスト設計..."
)

Task(
    description="UI Design",
    subagent_type="general-purpose",
    prompt="UI設計..."
)
```

#### Phase 3: 実装（完全並列）
```python
# 3つ同時に実行
Task(frontend_prompt)  # 15分
Task(backend_prompt)   # 10分
Task(database_prompt)  # 5分
# → 15分で完了（最長のFrontend時間）
```

#### Phase 4: 品質改善（ループ）
```python
for iteration in range(3):
    # 評価
    evaluation = Task(evaluator_prompt)

    if evaluation.passed:
        break

    # 改善計画と修正を並列
    Task(improvement_planner_prompt)
    Task(fixer_prompt)
```

## 📈 パフォーマンス比較

### 順次実行（現在）
```
Phase 1: 5分
Phase 2: 10分
Phase 3: 30分（15+10+5）
Phase 4: 15分
Phase 5: 10分
合計: 70分
```

### 並列実行（Task使用）
```
Phase 1: 5分
Phase 2: 7分（一部並列）
Phase 3: 15分（完全並列）
Phase 4: 10分（一部並列）
Phase 5: 10分
合計: 47分（33%短縮）
```

## ⚠️ 注意事項

### 1. Taskツール呼び出しの制限
- 同時実行数に制限がある可能性
- エラーハンドリングが必要

### 2. ファイル競合の回避
```python
# 各エージェントが異なるファイルを編集
Frontend: src/frontend/*
Backend: src/backend/*
Database: src/database/*
```

### 3. 結果の統合
```python
# すべてのTaskが完了してから統合
results = wait_for_all_tasks()
integrate_results(results)
```

## 🚀 次回プロジェクトでの実行方法

### Step 1: プロジェクト開始時の宣言
```markdown
「このプロジェクトはTaskツールを使用して並列処理を行います」
```

### Step 2: Phase実行時の明示
```markdown
「Phase 3を並列実行します。Frontend、Backend、Databaseの3つのTaskを同時に起動してください」
```

### Step 3: 実行確認
```markdown
「3つのサブエージェントが並列で作業していることを確認してください」
```

## 📝 チェックリスト

### 並列処理の確認
- [ ] Taskツールを使用しているか
- [ ] 複数Taskを同一メッセージで起動しているか
- [ ] サブエージェントが独立して動作しているか
- [ ] 実行時間が短縮されているか

### トラブルシューティング
- [ ] ファイル競合は発生していないか
- [ ] 依存関係は正しく設定されているか
- [ ] クリティカルパスを優先しているか

## 💡 まとめ

**問題**: Taskツール未使用 → すべて順次実行
**解決**: Taskツール使用 → 並列実行可能
**効果**: 30-40%の時間短縮

**重要**: 次回のプロジェクトでは、各フェーズで明示的にTaskツールを使用することを宣言してください。